# Java Practice source

This folder contains the editable Java source from the uploaded project used by the IntelliJ simulator.

- The simulator's browser-ready project snapshot lives in `/project-data/`.
- The default full-code preview opens `src/Threads/TwoThreads/MyThread.java`.
- No learning stages have been authored yet.
- The uploaded archive also contained compiled `.class` files; those are represented in the simulator project tree but are not duplicated here as source files.
