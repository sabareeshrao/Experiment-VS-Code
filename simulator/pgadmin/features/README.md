# pgAdmin 4 detailed feature catalog

One JSON file equals one feature/action-level capability. Read the relevant file before deciding transcript support. Candidate action mappings are heuristic and must be confirmed in the engine.
